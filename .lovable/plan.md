

# Bouton "Aider quelqu'un d'autre" — Accent ambre/or + HeartHandshake

## Modification

### `src/components/PostDonSocialBlock.tsx`
- Importer `HeartHandshake` depuis `lucide-react`
- Remplacer le `<Button variant="outline">` par `<Button className="bg-accent text-accent-foreground hover:bg-accent/90">`
- Ajouter `<HeartHandshake className="h-4 w-4 mr-2" />` avant le texte

1 fichier modifié.

